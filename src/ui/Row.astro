---
const { label, hour, isFirst=false } = Astro.props;
---
<div class={`flex align-middle ${ !isFirst ? 'border-t border-t-gray-400' : '' }`}>
	<div class="flex flex-1 py-2 px-4">{label}</div>
	<div class="flex py-2 px-4">{hour}</div>
</div>